apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ssd-ai-orchestrator-secret
  namespace: ssd-dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secretsmanager
  target:
    name: ssd-ai-orchestrator-secret
  dataFrom:
    - extract:
        key: ssd/dev/ai
