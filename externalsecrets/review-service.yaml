apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ssd-review-service-secret
  namespace: ssd-dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secretsmanager
  target:
    name: ssd-review-service-secret
  dataFrom:
    - extract:
        key: ssd/dev/review
