apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: ssd-api-gateway-secret
  namespace: ssd-dev
spec:
  refreshInterval: 1h
  secretStoreRef:
    kind: ClusterSecretStore
    name: aws-secretsmanager
  target:
    name: ssd-api-gateway-secret
  dataFrom:
    - extract:
        key: ssd/dev/common
